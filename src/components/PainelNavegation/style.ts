import { StyleSheet } from "react-native";

export const Painel = StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "flex-end",
    },

    bolinhas: {
        margin: 30
    }
})