import { StyleSheet } from "react-native";

export const Painel = StyleSheet.create({
    fone: {
        width: 200,
        height: 200
    },

    texto: {
        fontSize: 40,
        color: "#BFBCBC",
        
    },

    navegacao: {
        alignItems: "flex-end"
    },

    container: {
        flex: 1,
        alignItems: "center",
        justifyContent: "center"
    }
})